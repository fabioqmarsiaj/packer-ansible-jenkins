{
  "builders": [
  {
    "type": "docker",
    "image": "openjdk:8-jre-alpine",
    "commit": "true",
    "run_command": ["-d", "-i", "-t", "{{.Image}}", "/bin/sh"],
    "changes": [
      "EXPOSE 8080",
      "ENTRYPOINT [\"java\",\"-jar\",\"/usr/local/desafio-final/app.jar\"]"
    ]
  }],
 "provisioners": [
   {
     "type": "ansible",
     "playbook_file": "antonio-azambuja/temafinal1-cloud/tasks/infra/playbook.yml"
   }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "fabioqmarsiaj/cloudnative",
        "tag": "latest"
      },
      {
        "type": "docker-push",
        "login": "true",
        "login_username": "{{user `docker_user`}}",
        "login_password": "{{user `docker_password`}}"
      }
    ]
  ]
}
